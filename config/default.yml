# =============================================================================
# Default Runner configuration
# =============================================================================
globals:
    options:
        working-dir:
            config: working_dir
            mode: 2
            description: Working directory, defaults to current working directory.
            default: .

        bin:
            mode: 2
            description: The Taskman binary
            default: bin/taskman
