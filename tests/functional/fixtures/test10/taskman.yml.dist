env:
    FOO: BAR

commands:
    test:global-env:
        preconditions: test $FOO = BAR
        tasks:
            - echo "FOO === BAR"
            - echo $LOCAL
    test:local-env:
        env:
            LOCAL: LOCAL is a local environment variable
        tasks:
            - echo "FOO === BAR"
            - echo $LOCAL
